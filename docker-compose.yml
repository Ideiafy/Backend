version: '3.7'

services:
  postgres:
    image: bitnami/postgresql:latest
    ports:
      - '5434:5432'
    env_file:
      - .env
    volumes:
      - polls_pg_data:/bitnami/postgresql


volumes:
  polls_pg_data: